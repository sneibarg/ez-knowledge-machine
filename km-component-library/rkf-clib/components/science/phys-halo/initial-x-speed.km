;;
;; $Id: initial-x-speed.km,v 1.4 2008/07/25 17:52:38 kbarker Exp $
;;

(initial-x-speed has
  (instance-of (EventProperty))
  (superslots ())
  (subslots ())
  (domain (Move))
  (range (Speed-Value))
  (scale-class (Velocity-Scale))
  (scalar-constant-class (Velocity-Constant))
  (cardinal-unit-class (UoM-Speed))
  (inverse (initial-x-speed-of))
  (cardinality (N-to-1))
  ; (inherit-with-overrides (t))
  (fluent-status (*Inertial-Fluent))
)

(initial-x-speed has (wn20-synset ((:set 
				       (:triple "initial x speed" 0 "n")
;				       (:triple "initial x velocity" 0 "n")
				       (:triple "initial horizontal speed" 0 "n")
;				       (:triple "initial horizontal velocity" 0 "n")
))))

(initial-x-speed-of has (instance-of (EventProperty)))
(initial-x-speed has (name ("initial horizontal speed")))
